import React from "react";

const Loading = () => {
  return (
    <div className="w-full h-screen fixed z-[9999] flex justify-center items-center">
      loading...
    </div>
  );
};

export default Loading;
